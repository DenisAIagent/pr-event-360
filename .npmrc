# Force l'installation des devDependencies même quand NODE_ENV=production
# (cas des plateformes type Railway/Railpack). Nécessaire car le build du
# client utilise vite/tsc, qui sont des devDependencies.
include=dev
